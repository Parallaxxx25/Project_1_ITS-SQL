# ── FastAPI Core ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ── Database ──
sqlalchemy==2.0.36
greenlet>=3.0
aiosqlite==0.20.0
asyncpg==0.30.0
alembic==1.14.1

# ── Authentication ──
# PyJWT is the only JWT library: auth_service.py mints and decodes,
# middleware/auth.py verifies through the same decode_token().
pyjwt==2.10.1
httpx==0.28.1
google-auth==2.37.0
bcrypt==4.2.1

# ── Utilities ──
pydantic[email]==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
